{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur le meilleure réseau{% endblock %}

{% block body %}
<div class="container">
    <div class="jumbotron mt-4">
        <h1 class="display-3">Bienvenue sur ScpieBNB!</h1>
        <p class="lead">Nous mettons en relations les voyageur et des proprios qui veulent gagner de l'argent en proposant leurs bien à la location!</p>
        <hr class="my-4">
        <p>Vous êtes voyageur? et vous voulez voir ou vous pourrez loger?</p>
        <p class="lead">
        <a class="btn btn-primary btn-lg" href="{{ path('ads_index') }}" role="button">Voir nos annonces !</a>
        </p>
  </div>
  <h2 class="h1 my-5">Nos appartement stars</h2>
  <div class="row">
      {% for data in ads %}
        {% include 'ad/_ad.html.twig' with {'ad': data.annonce} %}
      {% endfor %}
  </div>
  <h2 class="h1 my-5">Nos propriétaires stars</h2>
  <div class="row">
      {% for data in users %}
        <div class="col">
            <div class="card border-secondary mb-3">
                <div class="card-body">
                        <h4 class="card-title">
                            {{data.user.fullName}}
                        </h4>
                    <hr>
                    <div class="card-text clearfix">
                        <img src="{{data.user.picture}}" class="avatar-medium" alt="Avatar de {{data.user.fullName}}" class="float-left mr-3">
                        {{data.user.introduction}}
                        <a href="{{ path('user_show',{'slug': data.user.slug}) }}" class="btn btn-success float-right">En savoir plus</a>
                    </div>
                </div>
            </div>
        </div>
      {% endfor %}
  </div>
</div>
{% endblock %}
