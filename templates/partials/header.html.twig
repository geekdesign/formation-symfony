<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('homepage') }}">ScpieBNB</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link" href="{{ path('homepage') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('ads_index') }}">Annonces</a>
        </li>
      </ul>
      <ul class="navbar-nav ml-auto">
        {% if app.user %}
        <li class="nav-item dropdown mr-1">
            <a class="nav-link dropdown-toggle" href="#" id="accountDropdownMenuLink" role="button" data-toggle="dropdown">
              <img src="{{app.user.picture}}" class="avatar avatar-mini mr-2" alt="Avatar de {{app.user.fullName}}">
              {{app.user.fullName}}
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownMenuLink">
                <a href="{{path('ads_create')}}" class="dropdown-item"><i class="fas fa-plus mr-1"></i>Ajouter une annonce</a>
                <div class="dropdown-divider"></div>
                <a href="{{path('account_bookings')}}" class="dropdown-item"><i class="far fa-list-alt mr-2"></i></i>Mes réservations</a>
                <div class="dropdown-divider"></div>
                <a href="{{path('account_index')}}" class="dropdown-item"><i class="fas fa-user mr-2"></i>Voir mon profil</a>
                <a href="{{path('account_profile')}}" class="dropdown-item"><i class="fas fa-user-edit mr-1"></i>Modifier mon profil</a>
                <div class="dropdown-divider"></div>
                <a href="{{path('account_logout')}}" class="dropdown-item"><i class="fas fa-sign-out-alt mr-1"></i>Déconnexion</a>
            </div>
          </li>
        {% else %}
        
            <li class="nav-item">
              <a href="{{path('account_register')}}" class="nav-link"><i class="fas fa-user-plus mr-1"></i>Inscription</a>
            </li>
            <li class="nav-item">
              <a href="{{path('account_login')}}" class="nav-link"><i class="fas fa-sign-in-alt mr-1"></i>Connexion</a>
            </li>
        {% endif %}
      </ul>
    </div>
  </nav>
{# Design des affichage des messages flash #}
{% for label, messages in app.flashes %}
  <div class="container">
    <div class="alert alert-{{label}}">
      {% for message in messages %}
        <p>{{ message | raw}}</p>
      {% endfor %}
    </div>
  </div>
{% endfor %}